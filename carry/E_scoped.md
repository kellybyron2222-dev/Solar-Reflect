# E scoped (from D)

Look-back D passed. Opened `carry/D_to_E.md` and `carry/C_to_D.md`. Frozen intent: terminator-nadir, \(D=h\alpha\), focusing does not shrink \(D_{\min}\). Trusted at 625 km: \(D\) and \(I\) (C). Weak: \(T_\text{useful}\).

## Diff vs default `phases/E_campaigns.md`

| Default | Decision |
|---|---|
| E1 Geometry | **Now** — S-geo-2d, S-spot-h, S-focus all exist. Do not skip E1. |
| E2 Peak \(I\) | **After E1 mini look-back.** Build S-I-A. No \(I^*\) lines. lux not a kernel output. |
| E3 Required \(A\) | **After E2.** A5 \(I^*\) only. |
| E4 Dwell + fluence | **Keep** — \(T\) weakly tested; stamp “as implemented in spec.” Build S-dwell in E4, not now. |
| E5 Readout | **Last.** Peak and duration separate columns. Optional lux as derived then. |
| E6 / extra campaign | **Cut** |
| M1km vs \(A_\text{image}\) | **E2 flag** (C watch). Not an E1 fail. |

## This campaign (E1)

Table \(D(h)\), \(A_\text{image}(h)\) on the A3 grid + GEO one-liner. Kernel `solar_image` only. Cite existing figures. No new scene. No \(I\), no lux, no \(I^*\), no verdicts.

## Will not do in E1

S-I-A, S-A-req, S-dwell. Application classes. Treating \(D\) as a pointing spec or a curvature knob. “So farms cannot….” Unlocking S-geo-2d.
